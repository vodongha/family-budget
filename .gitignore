# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Env & secrets
.env
.env.*
!.env.example
# Local secret-provisioning helper (reads .env/wallet at runtime — never commit)
scripts/fly_set_secrets.ps1

# Oracle ADB wallet — NEVER commit
wallet/
*.sso
*.p12
*.jks
tnsnames.ora
sqlnet.ora
ojdbc.properties

# Local data
*.sqlite
*.db
.DS_Store

# Admin panel CSS — compiled from styles/*.scss at startup (build artifact)
app/domains/admin/static/admin.css
